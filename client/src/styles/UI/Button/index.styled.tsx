import styled from "styled-components";

const Button = styled.button`
  padding: 20px 40px;
  font-size: 18px;
  font-weight: 600;
  outline: transparent;
  cursor: pointer;
  transition: all 0.3s ease-out;
`;

export { Button };
